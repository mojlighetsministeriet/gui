{
  "name": "mojlighetsministeriet-intranet",
  "license": "GPLv3",
  "devDependencies": {
    "bower": "^1.8.2",
    "eslint": "^4.10.0",
    "eslint-config-google": "^0.9.1",
    "eslint-plugin-html": "^3.2.2",
    "polymer-cli": "^1.5.7"
  },
  "scripts": {
    "postinstall": "./node_modules/.bin/bower install --allow-root",
    "lint": "yarn lint:javascript && ./node_modules/.bin/polymer lint",
    "lint:javascript": "./node_modules/.bin/eslint . --ext js,html --ignore-path .gitignore",
    "test": "./node_modules/.bin/polymer test",
    "build": "./node_modules/.bin/polymer build",
    "test:integration": "./node_modules/.bin/polymer build # test that psk builds without error with the CLI"
  }
}
